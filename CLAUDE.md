# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Quick Commands

- **`pnpm dev`** - Build and run TypeScript with esbuild
- **`pnpm dev:watch`** - Watch src directory and rebuild/rerun on changes (using nodemon)
- **`pnpm build`** - Compile TypeScript to JavaScript in `./dist` directory
- **`pnpm typecheck`** - Type-check without emitting code (using tsc)
- **`pnpm lint`** - Run ESLint on src directory
- **`pnpm lint:fix`** - Run ESLint with auto-fix enabled
- **`pnpm test`** - Run tests once with Vitest
- **`pnpm test:watch`** - Run tests in watch mode with Vitest
- **`pnpm format`** - Format all code using Prettier
- **`pnpm format:check`** - Check code formatting without making changes

## Project Structure

This is a minimal TypeScript template project. The structure is straightforward:

- **`src/`** - All TypeScript source files live here
- **`dist/`** - Compiled JavaScript output (generated by `pnpm build`)
- **`tsconfig.json`** - TypeScript compiler configuration with strict mode enabled
- **`package.json`** - Node.js project metadata and dependencies
- **`.node-version`** - Node version for nodenv (v22.16.0)
- **`.nvmrc`** - Node version for NVM (v22.16.0)

## Technology Stack

### Core

- **TypeScript 5.9.3** - Language and compiler with strict mode
- **esbuild 0.27.3** - Fast JavaScript bundler and compiler
- **tsx 4.21.0** - TypeScript executor for running TypeScript files
- **@types/node 25.3.0** - Type definitions for Node.js APIs

### Build & Development Tools

- **nodemon 3.1.14** - Monitor file changes and restart process during development
- **Vitest 4.0.18** - Fast unit test framework
- **ESLint 10.0.1** - Code linting with TypeScript support
- **Prettier 3.8.1** - Code formatter for consistent style
- **Lefthook 2.1.1** - Git hooks manager for automated checks
- **lint-staged 16.2.7** - Run linters on staged files
- **pnpm 10.26.1** - Package manager (see `packageManager` field in package.json)

### Dependencies

- **@t3-oss/env-core 0.13.10** - Type-safe environment variable management
- **dotenv 17.3.1** - Load environment variables from .env files
- **Zod 4.3.6** - TypeScript-first schema validation
- **es-toolkit 1.44.0** - Utility library with modern JavaScript functions

## Development Notes

- **Node.js 22.16.0** is required (specified in `.node-version` and `.nvmrc` for version managers)
- TypeScript strict mode is enabled, so all files must have proper type annotations
- Source code should be written in the `src/` directory with `.ts` extension
- Pre-commit hooks are configured with Lefthook:
  - Runs ESLint and Prettier on staged TypeScript files
  - Run `pnpm prepare` to install hooks (automatically run on `npm install`)
- Use `pnpm dev:watch` for fast iteration with auto-rebuild and auto-rerun
- Use `pnpm test:watch` during development for test-driven development
- Use `pnpm lint` to check for code issues before committing
- Use `pnpm build` before deployment or when you need compiled JavaScript output
- Use `pnpm typecheck` to verify type safety without building

## Code Formatting

Prettier is configured for consistent code formatting:

- Configuration is in `prettier.config.js`
- Run `pnpm format` to format all files
- Run `pnpm format:check` to verify formatting without changes
- Files in `.prettierignore` are excluded from formatting
